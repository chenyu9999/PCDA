function resolve(m) {
    var list = "Array" === m.constructor ? m : [];
    return (({
        modules: list,
    }))
};

function r(d1, d2) {
    if (d1 === undefined || d1 === null) {
        if ("string" === typeof d2 && d2[(0)] === "{" && d2[((nt_1 = (d2.length - 1), null == nt_1 ? undefined : 'number' === typeof nt_1 ? nt_1 : "" + nt_1))] === "}") {
            return (JSON.parse(d2))
        };
        return (d2)
    };
    return (d1)
};
module.exports = ({
    r: r,
    resolve: resolve,
});